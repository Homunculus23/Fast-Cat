import { defineComponent } from "vue";
export const Icon = defineComponent({
    setup: (props, context) => {
        return () => (
            <div>
                
            </div>
        );
    }
})